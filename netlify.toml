[build]
  functions = "netlify/functions"

[functions]
  # Function timeout (max 10 seconds on free tier)
  timeout = 10

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200